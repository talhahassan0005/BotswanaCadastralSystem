# Secrets — never commit
.env
.env.local
.env.*.local
*.key

# Node
node_modules/
.next/
out/
dist/
build/
*.log
npm-debug.log*

# Python
services/engine/.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Uploads / generated artifacts
apps/api/uploads/
apps/api/exports/

# Private proposal / milestone doc — not for GitHub
MILESTONES.md

# Raw client source data / reference material — kept locally, not in the repo.
# (The app ships the generated apps/web/lib/refMarksData.ts; regenerate with
#  scripts/genRefMarks.ts when the CONTROL POINTS source changes.)
CONTROL POINTS/
screenshot/
